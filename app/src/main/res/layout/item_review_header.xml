<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">
    
    <LinearLayout android:id="@+id/insertReview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/backgroundPrimaryColor"
        android:gravity="center_vertical"
        android:layout_marginTop="@dimen/reviews_spacing"
        android:layout_marginBottom="26dp"
        android:orientation="horizontal"
        android:padding="@dimen/reviews_spacing">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/reviewProfilePic_CircleImageView"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginEnd="@dimen/reviews_spacing"
            android:src="#939393"
            android:visibility="gone"
            app:civ_border_color="?attr/dividerColor"
            app:civ_border_width="1dp"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/insertReview"
            android:textColorHint="?attr/textSecondaryColor" />

    </LinearLayout>

</LinearLayout>